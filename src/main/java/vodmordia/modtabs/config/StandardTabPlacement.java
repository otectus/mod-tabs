package vodmordia.modtabs.config;

/**
 * User-facing placement override for tabs registered with GUI_RELATIVE positioning.
 * Tabs explicitly registered as SCREEN_TOP, SCREEN_BOTTOM, SCREEN_RIGHT, or CUSTOM
 * are not affected by this setting.
 */
public enum StandardTabPlacement {
    TOP,
    BOTTOM,
    RIGHT
}
